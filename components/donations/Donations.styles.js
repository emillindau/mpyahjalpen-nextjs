import styled from 'styled-components';

export const DonationsCardWrapper = styled.div`
  background-color: ${props => props.theme.colors.white};
  padding: 2em;
`;

export const StickerWinnerWrapper = styled.div`
  position: absolute;
  left: -40px;
  top: -85px;
  transform: rotate(-45deg);
`;
